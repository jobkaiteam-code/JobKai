apiVersion: apps/v1
kind: Deployment
metadata:
  name: footprint-service
  namespace: jobkai
  labels:
    app: footprint-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: footprint-service
  template:
    metadata:
      labels:
        app: footprint-service
        tier: backend
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
      - name: footprint-service
        image: jobkairegistry.azurecr.io/footprint:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8001
          protocol: TCP
        env:
        - name: GROQ_API_KEY
          valueFrom:
            secretKeyRef:
              name: jobkai-secrets
              key: GROQ_API_KEY
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8001
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: footprint-service
  namespace: jobkai
  labels:
    app: footprint-service
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: footprint-service
